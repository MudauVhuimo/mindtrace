# GEMINI_API_KEY: Required for the /api/generate-trace proxy (server-side only, never exposed to browser).
# Get one at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: PORT (defaults to 3000). Set by hosting platforms automatically.
# PORT=8080
